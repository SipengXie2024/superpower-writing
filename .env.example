# cn-bid-writing environment template
# Copy to .env (gitignored) for local notes if needed.

# Optional: path to a Pandoc reference docx can be recorded in .bid/metadata.yaml:
# export.reference_docx: "/absolute/path/to/reference.docx"

# Required command-line tools for full export:
# - python3 with PyYAML
# - pandoc for docx generation
