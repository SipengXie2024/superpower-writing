---
description: Initialize a .bid workspace for a Chinese bid document.
argument-hint: "[target dir, default .bid]"
---

Run `${CLAUDE_PLUGIN_ROOT}/scripts/init-bid-dir.sh ${ARGUMENTS}` to create the bid workspace. If no argument is provided, use `.bid`.

After initialization, summarize the created files and recommend `/bid:outline` as the next step.
