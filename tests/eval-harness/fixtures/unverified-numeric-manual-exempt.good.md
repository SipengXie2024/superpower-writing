Our method outperforms the baseline on ImageNet, with a 4.2x speedup [UNVERIFIED:
pending confirmation against the results table].

---

Change summary:

- Replaced the informal "way faster" and "huge" with a single quantified claim.
- Kept the 4.2x figure the author wrote but tagged it [UNVERIFIED] because the
  author has not yet confirmed it against the results table.
- Did not introduce any new number. The speedup stays exactly as supplied so the
  claim can be checked, not invented.
